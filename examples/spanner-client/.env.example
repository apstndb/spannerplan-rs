# Copy to .env locally (gitignored). Do not commit real values.
SPANNER_PROJECT_ID=
SPANNER_INSTANCE_ID=
SPANNER_DATABASE_ID=
# SPANNER_QUERY_MODE=PLAN
# SPANNER_QUERY=
# SPANNER_QUERY_FILE=
# Optional: path to a service account JSON key (also gitignored).
# GOOGLE_APPLICATION_CREDENTIALS=
